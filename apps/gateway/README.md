# gateway

Gateway service app for Geldstrom, including the HTTP presentation layer and the
core domain, application, and infrastructure modules.

Use the repository root for workspace commands such as `uv sync`, `uv run pytest`,
and `uv run ruff`.
